import styled from "styled-components";

export const Ul = styled.ul`
  justify-content: center;
  display: grid;
  max-width: calc(100vw - 48px);
  grid-template-columns: repeat(auto-fill, minmax(274px, 28px));
  grid-gap: 28px;
  margin-top: 0;
  margin-bottom: 0;
  padding: 0;
  list-style: none;
  margin-left: auto;
  margin-right: auto;
  margin-bottom: 100px;
`;
